#!/bin/sh
{{- if eq .profile "work" }}
if  command -v just >/dev/null 2>&1; then
    echo "just already installed"
else
    # pipx is guaranteed present by run_once_00-install-python-tooling.sh.
    pipx install rust-just
fi
{{- end }}
